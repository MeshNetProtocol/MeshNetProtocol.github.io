# Vendor Console 可执行 PRD（更新版）

版本：v2.0  
状态：进行中（用户主导设计 / AI 执行实现）  
最后更新：2026-02-25

---

## 1. 文档定位确认
本文件是**开发控制台界面的详细执行文档**，用于统一以下工作：
1. 产品范围定义
2. 信息架构与交互策略
3. 视觉实现约束
4. 当前进展记录
5. 下一步执行任务

结论：该文档继续作为主 PRD 使用，不清空为普通说明文。

---

## 2. 项目目标（当前仍有效）
构建一个 Web3 供应商控制台（非宣传页），区分两类工作流：
1. 私人供应商（Private）
- 本地工具化配置
- 不上链、不公开、不托管数据

2. 商业供应商（Commercial）
- 基于 MetaMask + Base 网络执行链上动作
- 用于注册、续费、管理与提现

---

## 3. 当前策略（已确认）

### 3.1 单页三段式结构
采用单页分区，不拆新页面：
1. Entry（入口选择）
2. Private Workspace（私人工作区）
3. Commercial Workspace（商业工作区）

### 3.2 私人工作区第一性原理
私人工作区本质是：
1. 生成 JSON 配置文件
2. 帮助用户理解配置用途
3. 指导用户部署到 VPS
4. 导出后供 App 导入

### 3.3 商业工作区当前策略
1. 商业流程依赖 MetaMask 与 Base 网络
2. 会话逻辑为“会话就绪可直进，不就绪需签名建立会话”（后续可继续调整）

---

## 4. 当前代码状态（真实进展）

### 4.1 已完成
1. `vendor-console.html` 已按用户最新要求回退为**静态设计板**。
2. 页面中已移除所有业务交互逻辑与功能按钮（仅保留 `Back`）。
3. 目前用于“你给设计、我做还原实现”的执行阶段。

### 4.2 当前界面用途
当前页面用于：
1. 视觉与信息架构讨论
2. 布局占位评审
3. 后续按设计稿逐步恢复功能

### 4.3 当前不包含
1. MetaMask 连接逻辑
2. 私人配置生成器逻辑
3. 商业链上交互逻辑
4. 状态机门控逻辑

---

## 5. 变更记录（本轮关键）
1. 先前已完成多轮高保真试作与交互实验。
2. 用户确认改为“用户主导设计，AI 执行实现”。
3. 用户要求清空交互后，页面已简化为静态结构。
4. 文档更新为与当前代码一致的执行状态。

---

## 6. 下一阶段执行计划（按顺序）

### 阶段 A：确定私人工作区 UE 结构（你主导）
目标：定稿“用户路径优先”的私人工作区。

交付物：
1. 私人工作区线框（低保真即可）
2. 关键流程文案（首屏引导、VPS 决策、导入说明）
3. 信息层级确认（优先显示 vs 次要显示）

### 阶段 B：按设计稿实现静态高保真
目标：先实现视觉与布局，不接真实逻辑。

交付物：
1. 私人工作区完整静态页面
2. 商业工作区静态骨架
3. 统一组件风格（按钮、卡片、提示条、步骤区）

### 阶段 C：恢复交互（假数据）
目标：验证可用流程与可理解性。

交付物：
1. 私人配置生成假交互
2. JSON 预览/复制/下载假交互
3. VPS 脚本入口与教程交互

### 阶段 D：恢复真实逻辑
目标：接入钱包与链上行为。

交付物：
1. MetaMask 连接 + 网络校验
2. 商业会话状态逻辑
3. 商业管理操作接入
4. 状态机与错误提示完善

---

## 7. 私人工作区内容要求（待实现清单）

### 7.1 首屏必须回答的问题
1. 什么是配置文件？
2. 配置文件导入 App 的哪个位置？
3. 用户有没有 VPS？如果没有，下一步怎么做？

### 7.2 功能区（后续按设计恢复）
1. `Create Config File` 主入口
2. VPS 准备状态分流（已有/没有）
3. AI 加速模板快速生成
4. 高级 JSON 编辑入口
5. Shell 脚本复制入口（占位）
6. VPS 资源推荐链接

### 7.3 内容策略
1. 新手路径：教程优先
2. 熟手路径：一键直达创建
3. 强约束：默认只开放 AI 加速服务，不开放通用服务

---

## 8. 设计与实现协作模式（本项目当前规则）
1. 你负责产品与 UI 判断（结构、优先级、文案方向）。
2. 我负责快速落地与前端实现（HTML/CSS/JS 执行）。
3. 每轮先改结构，再补交互，最后接真实逻辑。
4. 若你否定设计方向，直接回退并按新指令重做，不做争辩。

---

## 9. MCP 工作流建议（用于提升设计还原质量）
推荐使用 Figma MCP 作为设计源：
1. 你在 Figma 给出目标 Frame（或节点）。
2. 我通过 MCP 读取节点结构、样式、变量。
3. 按节点进行高保真还原并迭代。

预期收益：
1. 降低主观审美偏差
2. 降低“理解错布局”风险
3. 提升还原一致性与执行效率

---

## 10. 当前待办（下一次开始即执行）
1. 根据你下一版私人工作区草图，先恢复静态结构。
2. 仅保留你指定的模块与文案，不添加自主扩展。
3. 你确认静态稿后，再逐步恢复假交互。

