# Vendor Console 可执行 PRD

版本：v1.0  
作者角色：产品经理（兼交互定义）  
日期：2026-02-25

---

## 1. 文档目标
本 PRD 用于直接指导设计、前端、合约对接与测试执行，目标是交付一个可商用的 Web3 供应商控制台（非宣传页），覆盖供应商从接入到运营结算的完整流程。

本版要求：
1. 支持 Base Mainnet 与 Base Sepolia。
2. 明确区分私人供应商与商业供应商。
3. 商业供应商支持注册、续费、profile 管理、提现。
4. 私人供应商支持本地导入和本地更新，不上链。
5. 界面信息层级清晰，主流程高优先，次要能力分区处理。

---

## 2. 业务背景与边界

### 2.1 业务模型
1. 私人供应商（Private Supplier）
- 本地导入 profile（文件/URL）
- 本地编辑与保存
- 不上链、不公开检索、无注册费

2. 商业供应商（Commercial Supplier）
- 通过合约注册和续费
- 公开可检索
- 支持链上 profile 管理与提现

### 2.2 合约边界
1. ProtocolRegistry
- 收费/续费
- supplierId 唯一性
- owner -> supplier 索引
- 费率与 treasury

2. SupplierProfile
- owner / supplierId / metadataURI 管理
- withdraw / withdrawAll

### 2.3 协议规则
1. 商业续费固定 1 年。
2. 结算采用 Base + USDC 语义（展示与提示层面）。

---

## 3. 用户角色与核心诉求

| 角色 | 典型身份 | 核心诉求 | 核心风险 |
|---|---|---|---|
| 私人供应商 | 开发者、小团队 | 快速导入、低成本试运行、隐私优先 | 误以为会上链、JSON 格式错误 |
| 商业供应商运营者 | 服务商、运营负责人 | 稳定上链运营、可续费、可提现 | 网络错误导致交易失败、操作对象选错 |
| Owner/财务 | 管理员、资金负责人 | 权限安全、提现可预估、操作可控 | Owner 转移误操作、地址输入错误 |

---

## 4. 使用场景

### 4.1 私人供应商场景
1. 团队内部测试新 profile，不希望对外公开。
2. 从 URL 拉取 profile JSON，修改后本地保存。
3. 在未连接钱包的情况下也能进行本地资料处理。

### 4.2 商业供应商场景
1. 首次入驻：连接钱包 -> Base 网络就绪 -> 签名登录 -> 注册供应商。
2. 日常运营：刷新商业供应商列表，切换到目标供应商后进行 metadata 更新。
3. 周期维护：每年续费 1 次，防止过期。
4. 财务结算：提现前先看手续费预估，再执行 withdraw 或 withdrawAll。
5. 权限变更：公司钱包变更时，执行 owner 转移。

---

## 5. 产品目标与成功标准

### 5.1 产品目标
1. 用户在 3 秒内判断“当前是否可以链上操作”。
2. 将高频商业操作的路径压缩到 2~4 次点击。
3. 显著降低误操作（网络错误、未登录写操作、地址格式错误）。

### 5.2 成功标准（验收指标）
1. 未 BASE_READY 时，链上写操作不可触发。
2. 未 AUTHENTICATED 时，商业管理区域不可执行。
3. 所有交易行为均有 pending/success/error 反馈。
4. 私人供应商操作全程不依赖链上连接。

---

## 6. 信息架构（IA）

### 6.1 一级信息（首屏）
1. 钱包状态（地址/未连接）
2. 网络状态（当前链 -> 目标链 + Ready）
3. 商业关键指标
- 商业供应商数量
- 年费
- 提现费率
- Treasury

### 6.2 二级信息（工作区）
1. Commercial Workspace
- 列表选择
- 注册（弹窗）
- 续费
- profile 管理
- 提现

2. Private Workspace
- 文件导入
- URL 导入
- 本地 JSON 更新

### 6.3 非目标
1. Activity Timeline（本期移除）。

---

## 7. 页面与交互结构

### 7.1 顶部 Command Bar
功能：
1. 目标网络选择
2. 连接钱包并签名登录
3. 切换到目标 Base 网络
4. 断开会话
5. 当前状态反馈（auth/network/notice）

交互规则：
1. 若未连接钱包，显示 Disconnected。
2. 若链不匹配，显示 Not Ready，并引导切链。
3. 交易 pending 时显示全局提示。

### 7.2 左侧工作区导航
1. Overview
2. Commercial
3. Private

规则：
1. 切换仅改变内容面板，不刷新页面。
2. 关键状态与钱包状态保留。

### 7.3 Overview 面板
内容：
1. 商业关键指标卡片
2. 商业模式说明卡片
3. 当前选中商业供应商摘要

目标：
1. 让用户先看“是否可操作 + 当前资产/对象”。

### 7.4 Commercial Workspace
内容：
1. 商业供应商列表（可选择）
2. 注册入口（弹窗）
3. 续费模块
4. Profile 管理模块
5. 提现模块

规则：
1. 必须先选中供应商，才能执行续费/profile/提现。
2. 交易按钮遵守状态机门控。

### 7.5 注册弹窗
内容：
1. supplierId
2. metadataURI
3. 提交按钮
4. 状态提示

规则：
1. 校验必填项。
2. 提交成功后关闭弹窗，回到商业工作区并刷新列表。

### 7.6 Private Workspace
内容：
1. 文件导入
2. URL 导入
3. 本地 JSON 编辑与更新
4. 状态提示

规则：
1. 不触发链上调用。
2. JSON 必须为对象。

---

## 8. 状态机定义（强约束）

状态：
1. INIT
2. WALLET_CONNECTED
3. BASE_READY
4. AUTHENTICATED
5. COMMERCIAL_DATA_READY
6. TX_PENDING
7. TX_CONFIRMED
8. TX_FAILED

关键约束：
1. 未 BASE_READY：禁用所有链上写按钮。
2. 未 AUTHENTICATED：商业写操作不可执行。
3. TX_PENDING：当前交易相关按钮禁用并展示 loading。
4. TX 结束后回到 AUTHENTICATED 或 COMMERCIAL_DATA_READY。

---

## 9. 字段与校验规则

### 9.1 商业注册
1. supplierId
- 必填
- 前端基础校验：非空

2. metadataURI
- 必填
- 前端基础校验：非空

### 9.2 Owner 转移
1. newOwner
- 必填
- 必须是合法 EVM 地址

### 9.3 提现
1. amount
- 必填
- 必须 > 0
- 按 USDC 6 位精度转换

2. to
- 必填
- 必须是合法 EVM 地址

### 9.4 私人 profile
1. JSON 必须是对象（非数组）
2. 若无 supplierId，允许生成默认本地 ID

---

## 10. 链上调用映射

### 10.1 Registry 读
1. getSupplierCountByOwner
2. getSupplierHashesByOwner
3. getSupplier
4. isSupplierActive
5. getFeeConfig

### 10.2 Registry 写
1. registerCommercialWithAuthorization
2. renewCommercialWithAuthorization

### 10.3 SupplierProfile 写
1. setMetadataURI
2. transferOwner
3. withdraw
4. withdrawAll

说明：
1. `authorization` 由后端签发，前端只保留调用位与错误反馈。

---

## 11. 异常处理与用户提示规范

### 11.1 统一错误策略
1. 捕获钱包拒签（4001）并提示“请在钱包中确认”。
2. 捕获网络不匹配并提示“切换到目标 Base 网络”。
3. 捕获地址格式错误并提示“检查地址后重试”。
4. 捕获 HTTP/JSON 错误并提示“检查 URL 与 JSON 格式”。

### 11.2 文案要求
1. 每条错误提示必须包含下一步建议。
2. 不暴露难懂的底层异常堆栈给普通用户。

---

## 12. UI/视觉执行规范（给设计与前端）

### 12.1 视觉方向
1. 风格：专业 Web3 SaaS 控制台，不做营销海报风。
2. 布局：桌面双栏，移动单栏。
3. 组件：卡片、状态徽章、模态弹窗、分区操作面板。

### 12.2 状态色
1. success：绿色
2. warning：橙色
3. error：红色
4. pending：蓝色

### 12.3 可读性
1. 地址、hash、合约地址使用等宽字体。
2. 提供复制按钮。
3. 重点信息（可操作性）在首屏可见。

### 12.4 响应式
1. >=1100：侧栏 + 主区
2. 768~1099：侧栏横向按钮
3. <768：单栏堆叠

---

## 13. 开发任务拆解（可直接排期）

### 13.1 前端任务
1. 信息架构重构（shell + workspace + modal）
2. 状态机落地与按钮门控
3. 钱包连接/签名/切链
4. 商业供应商读写流程接入
5. 私人 profile 本地流程
6. i18n 文案补齐

### 13.2 合约对接任务
1. 明确 `authorization` 获取接口协议
2. 确认 withdraw / withdrawAll ABI 最终签名
3. 提供 mainnet/sepolia 最终地址配置

### 13.3 QA 任务
1. 网络与钱包状态切换用例
2. 注册/续费/profile/提现主流程用例
3. 私人模式导入与本地更新用例
4. 错误分支覆盖（拒签、链错误、地址错误、JSON 错误）

---

## 14. 验收清单（QA Checklist）

### 14.1 首屏与状态
1. 未连接钱包时，链上写按钮不可点击。
2. 连接后若链不匹配，提示切换并不可写。
3. 切换到目标链并签名后，可进入商业操作。

### 14.2 商业流程
1. 刷新列表后显示 count 与 supplier 列表。
2. 注册弹窗提交成功后自动刷新列表。
3. 选中供应商后可执行续费。
4. metadata 更新成功后可回显。
5. owner 转移地址校验生效。
6. 提现预览数值正确显示。

### 14.3 私人流程
1. 文件导入成功并可回显 JSON。
2. URL 导入失败有明确提示。
3. 本地 JSON 非对象时阻断更新并提示。

### 14.4 质量要求
1. 无明显前端报错。
2. 中英文切换无缺失键。
3. 移动端布局无关键功能遮挡。

---

## 15. 非功能与安全要求
1. 不引入大型 UI 框架。
2. 前端不保存敏感私钥信息。
3. 本地存储仅用于私人 profile 与界面偏好。
4. 写操作必须走钱包签名与链上确认。

---

## 16. 风险与后续规划

### 16.1 已知风险
1. authorization 服务未接入时，商业注册/续费可能受限。
2. 多供应商并发读取在大规模账户下可能有性能压力。

### 16.2 后续版本建议
1. 商业供应商分页与搜索。
2. 交易历史中心（替代 Timeline 的审计页）。
3. 多角色权限视图（Owner / Operator / Finance）。
4. 可观测性接入（Sentry + 业务事件埋点）。

---

## 17. 交付物清单
1. 本 PRD 文档（本文件）
2. 高保真 UI 设计稿（下一步）
3. 前端实现与测试报告（开发阶段）
4. 合约联调记录与验收报告（联调阶段）

---

## 18. Web3 UI 设计方案（追加）

角色：资深 Web3 UI 设计师  
目标：基于本 PRD 将控制台从“功能可用”升级为“商用品质可读、可控、可信”的运营工具界面。

### 18.1 设计总原则
1. 可操作性优先于装饰性
- 用户首要问题是“现在能不能发交易”，而不是“页面好不好看”。
- 所有视觉层级围绕 Ready/Auth/Tx 状态展开。

2. 风险感知前置
- 对链上写操作统一做强可视化门控（禁用、说明、状态色）。
- 交易前后状态必须具备清晰的阶段反馈，避免“点了没反应”。

3. 任务分区清晰
- 高优先任务（商业管理）与次要任务（私人 profile）分区，不混排。
- 单屏只呈现一个“主任务焦点”。

4. 信息密度可控
- 首屏只保留决策必需信息（钱包、网络、指标、当前对象）。
- 详细编辑类信息进入工作区或弹窗，减少认知负担。

### 18.2 品牌与视觉方向

#### 18.2.1 风格关键词
1. 可信（Trustworthy）
2. 执行型（Operational）
3. 链上金融感（On-chain Finance UI）

#### 18.2.2 视觉基调
1. 以浅色为主界面（便于长时间运营使用）
2. 通过深色文字 + 强对比状态色表达交易风险
3. 背景使用低饱和渐变与轻微噪点感（弱化“纯后台表单感”）

#### 18.2.3 字体建议
1. 正文字体：`Inter` / `Sora`（英文）与系统中文字体回退
2. 数值与地址：`JetBrains Mono` / `SF Mono`

### 18.3 版式与页面结构

#### 18.3.1 全局框架
1. Top Command Bar（固定关键状态与动作）
2. Left Workspace Nav（任务分区）
3. Main Workspace Panel（单任务上下文）
4. Modal Layer（高风险或一次性流程，如注册）

#### 18.3.2 信息层级策略
1. L1：运行条件信息
- 钱包连接状态
- 当前链 vs 目标链
- Base Ready

2. L2：业务关键指标
- 商业供应商数量
- 年费
- 提现费率
- Treasury

3. L3：操作对象上下文
- 当前选中供应商（supplierId / owner / profile / expiry / active）

4. L4：执行动作
- 注册、续费、metadata、owner、withdraw

### 18.4 核心页面设计思路

#### 18.4.1 Overview（运营总览）
目标：30 秒完成状态判断。  
组成：
1. 指标卡片（4 张）
2. 业务模式说明（Private vs Commercial）
3. 当前选中供应商摘要卡

交互要点：
1. 每张指标卡支持 hover 说明（tooltip）解释数据来源。
2. 当前供应商摘要卡提供“进入商业工作区”主 CTA。

#### 18.4.2 Commercial Workspace（高频操作区）
目标：减少误操作并提升连续操作效率。  
布局：
1. 左列：供应商列表（选择上下文）
2. 右列：3 个操作块
- 续费（单一主动作）
- Profile 管理（metadata + owner）
- 提现（预览 + 提交）

交互要点：
1. 列表项高亮必须强烈（边框 + 背景 + active dot）。
2. 未选择供应商时，右侧操作块显示占位说明，不展示可点击主按钮。
3. 提现区先做预览，再允许提现（推荐流程，不强制）。

#### 18.4.3 Register Modal（注册弹窗）
目标：压缩注册路径并强化“高价值动作”感知。  
内容：
1. supplierId
2. metadataURI
3. 提交按钮
4. 风险提示（注册费与链环境）

交互要点：
1. 表单错误就地提示（字段下方），不要只在顶部提示。
2. 提交后按钮进入 loading，并在标题区域显示 pending。
3. 成功后自动关闭并刷新列表，toast 提示“已注册”。

#### 18.4.4 Private Workspace（低风险本地区）
目标：保持“轻量、离线感、开发友好”。  
组成：
1. 文件导入
2. URL 导入
3. JSON 编辑器

交互要点：
1. 明确标识“不会上链”。
2. JSON 编辑区提供格式校验提示（行级错误优先，后续版本可加）。

### 18.5 组件规范（Design Tokens + Components）

#### 18.5.1 色彩角色
1. Primary：品牌主色（用于主 CTA）
2. Success：成功确认
3. Warning：网络不匹配、需注意状态
4. Error：失败与高风险动作
5. Pending：交易进行中

#### 18.5.2 状态徽章
1. 形态：圆角胶囊
2. 必备状态：Disconnected / Not Ready / Ready / Pending
3. 显示规则：文本 + 色块同时变化，不只靠颜色（保证可访问性）

#### 18.5.3 按钮分级
1. Primary：交易主动作（注册、续费、提现）
2. Secondary：辅助动作（刷新、预览、复制）
3. Danger：不可逆高风险（owner 转移、withdrawAll）

#### 18.5.4 卡片规范
1. 卡片头：标题 + 单句目的
2. 卡片体：字段或动作
3. 卡片尾：状态反馈（成功/错误/下一步）

### 18.6 状态反馈与交易体验

#### 18.6.1 反馈层级
1. Global Notice（全局状态条）
- 用于网络、签名、全局错误

2. Section Status（模块级状态）
- 用于注册/续费/profile/提现各自反馈

3. Field Error（字段级错误）
- 用于地址格式、必填项、JSON 结构

#### 18.6.2 TX Pending 体验
1. 当前按钮 loading + disabled
2. 同组按钮 disabled（避免重复提交）
3. 全局提示“等待钱包确认/等待链上确认”

#### 18.6.3 错误文案策略
1. 错误原因 + 下一步动作
2. 示例：
- “网络不匹配，请切换到 Base Sepolia 后重试”
- “地址格式无效，请检查 0x 地址”

### 18.7 响应式与可访问性

#### 18.7.1 响应式
1. Desktop：侧栏 + 主区双栏
2. Tablet：侧栏转顶部横向 tabs
3. Mobile：单栏堆叠，按钮全宽

#### 18.7.2 可访问性（A11y）
1. 颜色对比度满足 WCAG AA
2. 键盘可操作（Tab 焦点顺序清晰）
3. 模态支持 ESC 关闭与焦点陷阱
4. 状态信息不只依赖颜色（文本同步）

### 18.8 动效策略（克制）
1. 页面级：淡入 + 轻微上移
2. 状态级：徽章色彩平滑过渡
3. 交易级：按钮 loading 小动画
4. 禁止炫技型大动画，避免干扰运营决策

### 18.9 设计交付物建议（给设计团队）
1. 信息架构图（IA）
2. 低保真线框（Overview / Commercial / Private / Modal）
3. 高保真稿（Desktop + Mobile）
4. 组件库（按钮、卡片、徽章、状态条、列表项）
5. 交互标注稿（状态机与按钮禁用规则）
6. 文案规范表（成功/失败/下一步）

### 18.10 与开发协作约定
1. UI 设计不改变合约边界，只优化交互表达。
2. 所有关键状态必须可由前端状态机驱动。
3. 每个主动作至少有三态：idle / pending / done。
4. Danger 操作统一保留二次确认交互（后续迭代可加入 confirm modal）。

### 18.11 下一步视觉迭代路线图
1. v1.1：完成高保真统一视觉（卡片密度、字阶、状态条）
2. v1.2：增加交易结果面板（hash、区块浏览器快捷入口）
3. v1.3：支持批量供应商筛选与操作历史中心

